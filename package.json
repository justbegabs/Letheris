{
  "name": "solo-social-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Rede social solo com backend Node + SQLite",
  "main": "server.js",
  "scripts": {
    "setup": "node scripts/setup.js",
    "postinstall": "node scripts/setup.js",
    "start": "npm run setup && node server.js",
    "dev": "npm run setup && node --watch server.js",
    "reset-password": "node scripts/reset-admin-password.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^2.1.1",
    "pg": "^8.13.3"
  }
}
